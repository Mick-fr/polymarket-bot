# ══════════════════════════════════════════════════════════════
# POLYMARKET BOT – Configuration
# Copier ce fichier en .env et remplir les valeurs
# ══════════════════════════════════════════════════════════════

# ── Polymarket ───────────────────────────────────────────────
# Clé privée de ton wallet Polygon (SANS le préfixe 0x)
POLYMARKET_PRIVATE_KEY=

# Adresse du wallet qui détient les fonds (nécessaire si proxy wallet)
# Laisser vide si tu utilises un wallet EOA standard (signature_type=0)
POLYMARKET_FUNDER_ADDRESS=

# Type de signature : 0=EOA standard, 1=email/Magic, 2=browser proxy
POLYMARKET_SIGNATURE_TYPE=0

# ── Bot ──────────────────────────────────────────────────────
# Intervalle entre chaque cycle (en secondes)
BOT_LOOP_INTERVAL=60

# Taille max d'un ordre (en USDC)
BOT_MAX_ORDER_SIZE=5.0

# Perte quotidienne max avant arrêt automatique (en USDC)
BOT_MAX_DAILY_LOSS=10.0

# Tentatives de reconnexion avant pause longue
BOT_MAX_RETRIES=5

# Délai entre les tentatives (en secondes)
BOT_RETRY_DELAY=30

# Paper trading : true = simule les ordres sans les envoyer à Polymarket
BOT_PAPER_TRADING=false

# Solde fictif initial en USDC (uniquement utilisé si BOT_PAPER_TRADING=true)
BOT_PAPER_BALANCE=1000.0

# Exposition nette maximale par marché (fraction du solde disponible)
# Augmenter si le portefeuille est petit (< 100 USDC).
# Ex : 0.20 = 20% du solde | 0.30 = 30% | défaut : 0.20
BOT_MAX_EXPOSURE_PCT=0.20

# Stop-loss par position : coupe une position si perte latente >= seuil
# (avg_price_entrée - mid_actuel) / avg_price_entrée >= BOT_STOP_LOSS_PCT
# Déclenche un SELL market (FOK) immédiat au lieu d'attendre un SELL limit.
# Ex : 0.25 = coupe si -25% de perte | 0.0 = désactivé | défaut : 0.25
BOT_STOP_LOSS_PCT=0.25

# ── Dashboard ────────────────────────────────────────────────
# Port du dashboard web
DASHBOARD_PORT=8080

# Clé secrète Flask (générer avec : python -c "import secrets; print(secrets.token_hex(32))")
DASHBOARD_SECRET_KEY=

# Hash bcrypt du mot de passe admin
# Générer avec : python -c "import bcrypt; print(bcrypt.hashpw(b'ton_mot_de_passe', bcrypt.gensalt()).decode())"
DASHBOARD_PASSWORD_HASH=

# ── Général ──────────────────────────────────────────────────
# Niveau de log : DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO
